# Action Catalog

This document lists every action the harness accepts via
`nevsky do <action>` (CLI) or its programmatic equivalent. Each
action has a canonical name, the rule it implements, and the
arguments it expects.

To be populated as actions are implemented in subsequent phases.
